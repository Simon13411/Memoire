Comment lancer??

Boite)	D'abord lancer "TestExecuteFillDb.py" avec comme fichier excel "GoodFormat.xlsx"
	Ce fichier contient toutes les valeurs qui passe le filtering ("TestFiltering")
	
	Ce script va retourner plusieurs soucis.
		a) Si pas les bonnes colonnes (pas notre cas pour le moment) retourne un String "pas les bonnes colonnes"
			A voir comment tu veux que ça soit retourné.
		b) Si erreur dans le filtering retourne a = List(bad_index) et b=nb_bad_index
			Les raisons des erreurs sont encore à faire
		c) Si pas d'erreur retourne le pandas DataFrame
	lors de l'execution des scripts, il faut assigner la variable "admin" à True ou False 

Individu)	Ensuite lancer "exececuteFillIndividuTest.py" avec comme fichier excel "templateIndividuBis.xlsx"
	Ce fichier contient aussi toutes les valeurs qui passe le filtering ("IndividuFilteredTest")
		a) Dans un premier temps, on appelle "boxexiste.py" qui va regarder si la boite de l'individu existe
			si la longueur est plus grande que zero alors erreur		
		b) Si pas les bonnes colonnes (pas notre cas pour le moment) retourne un String "pas les bonnes colonnes"
			A voir comment tu veux que ça soit retourné.
		c) Si erreur dans le filtering retourne a = List(bad_index) et b=nb_bad_index
			Les raisons des erreurs sont encore à faire
		d) Si pas d'erreur retourne le pandas DataFrame
	lors de l'execution des scripts, il faut assigner la variable "admin" à True ou False 
