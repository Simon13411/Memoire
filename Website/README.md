**WARNING**

You must change the IP which is in environment variable in the Dockerfile of the front-end by the IP of the server of your Gateway.

You also need to change the kind of environment you will use regard your needs (production or dev)

In dev environment you will need to use the makefile to run front-end container. If not, use the docker-compose commands.